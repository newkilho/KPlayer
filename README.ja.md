[한국어](README.md) | [English](README.en.md) | **日本語** | [简体中文](README.zh-CN.md)

# KPlayer

![Platform](https://img.shields.io/badge/platform-Windows-blue)
![Engine](https://img.shields.io/badge/engine-libmpv-green)
![License](https://img.shields.io/badge/license-GPL--2.0--or--later-lightgrey)

広告もアドウェアも余計なものもない、すっきりした Windows メディアプレイヤー。  
オープンソースのメディアエンジン **libmpv** ベース。

![KPlayer](https://kilho.net/wp-content/uploads/2026/03/K-949.png)

## 機能

- **38 形式** — 動画 23 種 (MP4, MKV, AVI, MOV, WMV, WebM, TS, M2TS, VOB, RM/RMVB など)、
  音声 12 種 (MP3, FLAC, AAC, M4A, WAV, OGG, Opus, WMA, APE, DSF など)、プレイリスト 3 種 (M3U, M3U8, PLS)
- ファイルやフォルダをドロップするだけで追加 — 本体ウィンドウはリストを置き換え、プレイリストウィンドウは追記
- ドラッグでプレイリストの順序変更、リピート再生とシャッフル再生、プレイリストの保存
- 字幕の表示/非表示とトラック切替、フォント・色・縁取り・影・位置の設定
- チャプター移動、フレーム単位の移動、再生速度 (0.25×–4.0×)、スクリーンショット (PNG/JPG)
- `TAB` 情報パネル — ファイル・コーデック・解像度・フレームレートなど
- ハードウェアデコード、出力ドライバー・グラフィック API・画面同期・アップスケーラー・インターレース解除の選択
- 音量ノーマライズ (強度を選択)
- ファイルの関連付け — 設定で拡張子ごとに登録し、Windows の既定のアプリ選択画面を直接開く
- **ショートカットキーとマウス操作をすべてカスタマイズ可能** (設定 → ショートカット / マウス)
- 多言語 — 韓国語・英語・日本語・中国語・ロシア語・イタリア語・フランス語・スペイン語・アラビア語 (OS の言語に自動追従)
- フレームレスのダーク UI、常に手前に表示、新バージョンの自動確認

## 既定のショートカット

すべて設定 → **ショートカット** で変更できます。固定なのは `ESC` (全画面解除) と `TAB` (情報パネル) だけです。

| キー | 動作 |
|-----|--------|
| `Space` | 再生 / 一時停止 |
| `Enter` | 全画面 |
| `←` / `→` | 5 秒戻る / 進む |
| `Shift` + `←` / `→` | 1 秒戻る / 進む (正確) |
| `Ctrl` + `←` / `→` | 前 / 次のチャプター |
| `,` / `.` | 前 / 次のフレーム |
| `↑` / `↓` | 音量 +5 / −5 |
| `0` / `9` | 音量 +2 / −2 |
| `M` | ミュート |
| `Page Up` / `Page Down` | 前 / 次のファイル |
| `V` | 字幕の表示 / 非表示 |
| `J` / `Shift` + `J` | 次 / 前の字幕トラック |
| `S` | スクリーンショット |
| `Z` | 速度 1.0× |
| `X` / `C` | 速度 −0.1 / +0.1 |
| `[` / `]` | 速度 −10% / +10% |

プレイリスト・設定・常に手前に表示には既定のキーがなく、コントロールバーのボタンから開きます (キーを割り当てることもできます)。

## マウス

設定 → **マウス** でイベントごとに機能を選びます (何もしない・全画面・画面いっぱい・再生/一時停止・次/前のファイル・前へ/後ろへ移動・音量を上げる/下げる)。

| イベント | 既定値 |
|-----|--------|
| 左ボタンのダブルクリック | 全画面 / 元の画面 |
| ホイール上 / 下 | 音量を上げる / 下げる |
| 左ボタンのシングルクリック、中ボタン | 何もしない |

ウィンドウはどこをドラッグしても移動できます。

## 設定

| カード | 項目 |
|-----|--------|
| 一般 | リピートモード・シャッフル・プレイリストの保存・スクリーンショットのフォルダ/形式・常に手前に表示・再生ウィンドウのサイズ |
| 映像 | ハードウェアデコード・出力ドライバー・グラフィック API・画面同期・アップスケーラー・インターレース解除 |
| 音声 | 既定の音量・音量ノーマライズ・強度 |
| 字幕 | 既定で表示・サイズ・既定の言語・フォント・太字・文字色・縁取り・影・縦位置・配置・字幕ファイルのスタイルを優先 |
| 関連付け | 拡張子ごとの登録・既定のアプリの選択 |
| ショートカット | 29 種の動作にキーを割り当て |
| マウス | クリック・ダブルクリック・中ボタン・ホイールの動作 |

設定は `KPlayer.ini`、プレイリストは `KPlayer.lst` として実行ファイルの隣に保存されます (ポータブル)。

## インストール

インストーラーは `%LOCALAPPDATA%\KPlayer` に配置します。実行ファイルの隣に `libmpv-2.dll`、`KPlayer.lua`、`Icon\` フォルダが必要です。  
ソースからビルドするには Delphi (VCL, Win64) と [LibMPVDelphi](https://github.com/nbuyer/libmpvdelphi)、[Virtual Treeview](https://github.com/JAM-Software/Virtual-TreeView)、[SVGIconImageList](https://github.com/EtheaDev/SVGIconImageList) が必要です。

## ライセンス

GNU GPL v2 以降 · オープンソース · 自由に再配布可能。  
使用しているオープンソースの一覧は `THIRD-PARTY-NOTICES.txt` にあります。

## 作者

**Kilho.net** · https://v2.kilho.net
